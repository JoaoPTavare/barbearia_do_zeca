import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'app_na_regua',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
